{
	"name": "public",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"build": "tsc && vite build",
		"dev": "vite",
		"preview": "vite preview",
		"clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
	},
	"dependencies": {
		"solid-duo": "workspace:*"
	},
	"devDependencies": {
		"ts-config": "workspace:*",
		"typescript": "^4.5.3"
	}
}
