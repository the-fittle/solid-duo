{
	"name": "ts-config",
	"main": ".",
	"version": "0.0.0",
	"private": true,
	"license": "MIT",
	"scripts": {
		"clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
	},
	"publishConfig": {
		"access": "public"
	}
}
